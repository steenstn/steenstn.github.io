var Viewport = (function () {
    function Viewport() {
        Viewport.x = 0;
        Viewport.y = 0;
        Viewport.oldx = Viewport.x;
        Viewport.oldy = Viewport.y;
    }
    return Viewport;
}());
